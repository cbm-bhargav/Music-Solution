import React from 'react';

const Spinner = () => (
  <div className="flex items-center justify-center">
    <div className="w-16 h-16 border-b-2 rounded-full border-primary animate-spin"></div>
  </div>
);

export default Spinner;
